let drink = "Chai";
// this drink, we didn't give annotations as string but also it will understand that it's string if we will try to re-assign then it accept string only.

// This called Inference

let cups = Math.random() > 0.5 ? 10: 5;

let bookName = "Lean Typescript";

let authorName:string = "John Mone"
// authorName = 2 this will give syntax error because it's annotation is string.
// string
// boolean 
// integer

// This called Annotations